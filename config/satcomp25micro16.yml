# SAT Competition 2025 Configuration, but with reduced resource limits

# Solver resource limits
solver_cputime: 500          # CPU time limit for solvers in seconds
solver_walltime: 700         # Wall time limit for solvers in seconds
solver_memory: 65536         # Memory limit for solvers in MB

# Checker resource limits
checker_cputime: 4500        # CPU time limit for checkers in seconds
checker_walltime: 7000       # Wall time limit for checkers in seconds
checker_memory: 65536        # Memory limit for checkers in MB

# Job scheduler configuration
scheduling:
  scheduler: slurm
  machine: cpuonly                  # machine/partition name for scheduler
  account: hk-project-test-p0026435 # Account name for scheduler
  reservation: sat2           # SLURM reservation; remove or set DIKE_RESERVATION= to disable
  tasks_per_node: 16                # Number of tasks per node
  jobname: satcomp25micro16         # Job name for scheduler
  workerinit: "source /home/hk-project-toolbox/fv2117/git/SustainableCompetition/venv/bin/activate; export PYTHONPATH=/home/hk-project-toolbox/fv2117/git/SustainableCompetition:$PYTHONPATH; cd /home/hk-project-toolbox/fv2117/git/SustainableCompetition"  # Initialization commands for each worker node

# Benchmark and solver files
benchmarks: ./satcomp25.instances.csv # Path to CSV containing benchmark hashes
solvers: ./satcomp25.solvers.two.csv      # Path to CSV containing solver paths
results: ./satcomp25micro.results16          # Path to directory for storing results
